<h1>All Groups</h1>
<%= link_to 'Create New Group', new_group_path %>
<% @all_groups.each do |group| %>
  <p>Name: <%= link_to group.name, group_path(group) %></p>
  
  <p>Icon: <%= content_tag(:i, '', class: "fas fa-#{group.icon}") %></p>
  <p>Created_at: <%= group.created_at %></p> <br>
<% end %>